export enum Environment {
  development = 'development',
  staging = 'staging',
  production = 'production',
}

export enum UserAccountStatus {
  PENDING = 'PENDING',
  ACTIVE = 'ACTIVE',
  SUSPENDED = 'SUSPENDED',
  IN_ACTIVE = 'IN_ACTIVE',
}
